> **Build-stage document, kept as history.** These are instructions to the agent that
> planned the build, written before it ran. Nothing here is needed to use the harness —
> see `USER-GUIDE.md`.

# Job Search Agent Harness — Doc 02: Plan-Mode Handoff (Stage 2)
**For:** Fable running in Claude Code **Plan Mode**, in `~/Documents/job-search-build/job-search-agent-harness`.
**Self-contained:** this file + `harness-01-architecture.md` + the systems you inspect are everything; assume no access to prior conversations. The owner is non-technical and hands-off: batch questions, prefer the recorded defaults (doc 01 §14) over asking, and end every session with a plain-language bullet summary.

## 0. Role and hard rules
You inspect, reconcile, and plan. **You write no production code in Plan Mode** — deliverables are documents only. Doc 01's architecture is the design; your job is to reconcile it against local reality and produce the Opus implementation plan. Deviations from doc 01 require evidence (file/behavior you inspected) and land in the reconciliation log, not silently in the plan.
1. **The private system is read-only evidence.** Do not modify `../job-search` in any way. FIRST ACTION after reading the docs: create a disposable snapshot — `git clone --local ../job-search ../job-search-ref` (fallback: filesystem copy) — and inspect ONLY the snapshot. Delete it at the end. Never copy personal content (career facts, applications, tracker rows, outcome data) into this repo or the plan; port *mechanisms*, sanitized.
2. Priority order = doc 01 header. No recommendation may weaken truth validation, privacy, data-loss prevention, or state persistence (the Ponytail guard rule).
3. Maintain `docs/PLAN-STATE.md` (checklist of the sections below; [~]/[x] with commits) so a cutoff session resumes losslessly; the owner's re-paste of the same kickoff resumes you.
4. Facts you assert must come from inspection this session or carry an explicit UNVERIFIED tag with a resolution step in the plan.

## 1. Inspection list (record findings per item in the audit)
**A. Private system snapshot** (`../job-search-ref`): overall structure vs the Aug-1 plan docs in its `docs/`; BUILD-STATE + handoff/session documents (these reveal what was actually built and what changed after); the evidence register + fact_check implementation as built; the intake/apply-any workflow as built; search-board integrations actually configured (incl. remote-job sources and any Canadian/local boards added); the tracker workbook generator + its notes/edits handling as actually solved; application archive layout; resume/cover-letter generation + template state; grounding flow as it runs today; Gmail/outcome/interview usage evidence; anything present that no prior scope mentions.
**B. Installed capabilities:** `claude mcp list`; `/plugin` inventory; `~/.claude` + project skills/commands; Toolbelt (which MCPs, the global rules block); Toolshelf (search it for: job boards, resume, xlsx, pdf, handoff/session, humanize — before designing anything custom); Codex present? (`codex --version`, plugin list if so). For each relevant capability: does the private system actually use it → does the harness need it → portable equivalent (dependency/plugin the PUBLIC product can install) → decision: install / incorporate-small / omit. Users never need the owner's Toolbelt/Toolshelf.
**C. Upstream repos, current state:** ai-job-search (release tag to pin; exact command inventory; which command files embed Claude-specific mechanics — this yields RUNTIME-MAP's real contents; `check_upstream_updates.py` usage; security_guards + gitignore model); Ponytail + Caveman (current install commands per runtime; verify Codex plugin flow); Humanizer (licence file, portability to Codex — resolve doc 01's UNVERIFIED). Current Codex CLI docs: AGENTS.md behavior, prompt stubs, MCP config, any usable context/usage telemetry; same telemetry question for Claude Code (statusline/hooks). Do not fabricate telemetry availability — record exactly what each runtime exposes.

## 2. Required outputs (all under `docs/`; this is the brief's A–O)
**A** Current-system audit (what the private system NOW contains; delta vs its own plan docs). **B** Installed-capability audit. **C** Reuse matrix — every important capability → reuse-upstream / port-from-private(sanitized) / install-dependency / adapt / build-minimal / omit, one line of justification each. **D** Final repository structure (exact tree; upstream-owned vs harness-owned vs generated/gitignored). **E** Canonical state model (facts, preferences, discovered jobs, application state, templates, archives, handoff — one owner per family; views listed). **F** Claude/Codex adapter design = RUNTIME-MAP.md draft containing only the genuinely required differences found in 1C. **G** Implementation phases — small enough to review; each with objective, files to inspect/create/modify, dependencies, tests, acceptance, rollback. **H** File-level responsibilities. **I** Dependency pinning approach (release tags for upstream; plugin versions; requirements). **J** Licence/attribution checklist per bundled/derived component + the NOTICE text. **K** Test matrix across both runtimes (Codex lane per doc 01 §14.1 default if Codex absent). **L** Fresh-install test using the demo candidate. **M** End-to-end test list — implement the brief's full list: onboarding, CV ingestion, resume-as-template, preference profile, one-board focused search, all-board search, remote + location boards, user-added board, closed-job rejection, URL/screenshot/PDF/pasted applications, transferable-skill framing, hard skip-filter, unsupported metric/credential rejection, research, humanize→re-ground, ATS parse, render QA, archive, tracker update, interview prep from exact submitted materials, context handoff, usage handoff where detectable, Claude→Codex and Codex→Claude continuation, interrupted-session recovery, demo privacy sweep, fresh install, attribution presence, upstream-update drill. **N** Final simplification audit (one YAGNI pass; delete before Opus sees it). **O** **Opus build handoff**: `IMPLEMENTATION-PLAN.md` (the contract: decided architecture, never-edit list, phases with mechanical gates and auto-continue-on-green, BUILD-STATE ritual, bounded repair rule — *a failing check is fixed in code or content, never by weakening the check* — stopping conditions, definition of done incl. the privacy sweep and release gate) **plus `OPUS-KICKOFF.txt`** — a paste-ready kickoff the owner uses verbatim after switching models; it must instruct Opus to read the plan, execute all phases hands-off, involve the owner only at listed 👤 moments, and resume from BUILD-STATE on the same paste.

## 3. Owner touchpoints you may plan for (and no others)
Windows permission clicks during installs · Codex presence answer (§14.1) · the release-gate "flip public?" confirmation · a demo-candidate end-to-end eyeball ("package looks right" — on the FICTIONAL candidate; the owner's own data never enters this repo) · optional Codex-side smoke if he has it. Everything else must be autonomous.

## 4. Stopping conditions
Private-system write about to happen · personal data about to enter the public repo or plan · upstream reality contradicts doc 01 on something load-bearing (surface with quotes) · a truth/privacy check would need weakening · licence conflict discovered. Otherwise: reconcile, decide per priority order, log, proceed.

## 5. Kickoff (owner pastes; byte-identical in doc 03)
```
Read docs/harness-01-architecture.md and docs/harness-02-planmode-handoff.md in full. You are
Fable in Plan Mode: inspect and plan only — write no production code. First snapshot the
private system read-only per handoff §0.1, then work the §1 inspection list, then produce
every §2 deliverable, ending with IMPLEMENTATION-PLAN.md and OPUS-KICKOFF.txt. Keep
docs/PLAN-STATE.md current so this same paste resumes a cut-off session. Prefer the recorded
defaults over asking me; batch any real questions into one message.
```
