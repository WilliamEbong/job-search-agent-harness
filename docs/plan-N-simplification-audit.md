# N — Final Simplification Audit (Ponytail pass over C–M)

One question per component: *what user problem remains unsolved without
this?* Items CUT here were removed from G/H before O was written.

## Cut in this pass
1. **`scrape-mode wrapper(s)` as multiple commands** (was in H) → collapsed to
   ONE `scrape.md` wrapper reading mode + overrides. Two wrappers solved
   nothing one can't.
2. **`.claude/skills/preference-engine/` as a separate skill** → folded into
   the `/setup` harness wrapper command. The interview is a sequence in one
   workflow, not a reusable capability; a skill directory added a file and an
   indirection. (H updated: the wrapper command owns it.)
3. **`scout-docs/` as a distinct directory** (was ambiguous in D) → the two
   knowledge files land in `docs/` like every other doc. No new directory.
4. **Telemetry hard thresholds in code** → the statusline mirror script only
   WRITES the JSON; thresholds live in the continuity command prose (easier to
   tune, nothing to configure). No config surface added.
5. **Separate `harness/privacy_sweep.py` AND `tools/harness_guards.py`** →
   examined for merge; kept separate deliberately: guards = CI structural
   check (fast, every commit), sweep = content scan (release gate). Different
   failure modes, different runtimes. NOT merged — but sweep loses its CI slot
   (guards cover CI; sweep runs at release gate + fresh-install test only).
6. **Demo template in `templates/`** (was in D) → the stock upstream template
   already ships; a second demo template proves nothing. Cut. Template-
   inference is tested against a demo-candidate PDF fixture instead.
7. **`examples/` example tracker rows** → generated by the fresh-install test
   itself; shipping static copies risks drift. Cut static copies; `examples/`
   keeps only the example posting fixture(s).

## Challenged and kept (with the unsolved problem)
- **Quad-format outputs**: recruiters ask for .docx; ATS portals want .pdf;
  .md powers drift-proofing. Proven privately. Kept (pandoc soft-dep).
- **Firecrawl integration**: owner directive + real private-system usage
  (stealth rung, non-CLI boards). Kept as optional.
- **Statusline mirror script**: the only real telemetry channel (B.4). Kept.
- **XLSX 4-tab workbook**: doc 01 §13 already adjudicated (CSV/HTML
  insufficient for the target user). Kept.
- **Caveman**: owner directive. Kept (one config flag disables).
- **jobbank-ca port**: only proven non-Danish local-board example; ToS-clean.
  Kept.
- **Demo candidate**: unshippable repo without it (fixtures, CI, screenshots,
  fresh-install test). Kept.
- **.codex prompt stubs**: thinnest possible Codex entry; AGENTS.md alone
  doesn't give per-workflow invocation. Kept.

## Standing YAGNI fence (unchanged from doc 01 §13)
No databases, vector stores, extra dashboards, event buses, schedulers,
auto-submit, microservices, agent bureaucracies, duplicate truth stores.
Every future addition faces this audit's question first, then Ponytail's
ladder.
